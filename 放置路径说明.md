# K230 文件放置路径

本目录已经按照 K230 的根目录结构整理。只需要上传 `sdcard` 和 `data` 两个目录里的内容，不要把“入k230”这一层复制到开发板。

## 路径对应关系

| 电脑上的文件或目录 | K230 目标路径 |
|---|---|
| `入k230/sdcard/main.py` | `/sdcard/main.py` |
| `入k230/sdcard/super_mario.py` | `/sdcard/super_mario.py` |
| `入k230/data/mario/` | `/data/mario/` |

资源目录必须保持下面的结构：

```text
/data/mario/
  manifest.json
  bg/          973 个 PNG
  maps/         63 个 JSON
  masks/       105 个 PNG
  sprites/     105 个 PNG
```

## 推荐复制顺序

1. 先把电脑上的 `入k230/data/mario` 复制到 K230 的 `/data`，最终确认得到 `/data/mario/manifest.json`。
2. 把 `入k230/sdcard/super_mario.py` 复制到 K230 的 `/sdcard`。
3. 最后把 `入k230/sdcard/main.py` 复制到 K230 的 `/sdcard`。
4. 等待所有资源复制完成，再按 K230 的 RESET 键。

不要形成这些错误路径：

```text
/data/data/mario
/data/mario/mario
/sdcard/sdcard/main.py
/入k230/data/mario
```

正确的最终文件是：

```text
/sdcard/main.py
/sdcard/super_mario.py
/data/mario/manifest.json
```

## 固件升级前备份

重新烧录 `.img` 固件可能覆盖 TF 卡内容。升级固件前，应把 K230 上的 `/sdcard` 和 `/data/mario` 备份到电脑。
